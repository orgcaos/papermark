VERSION 3

NODE endpoint
SQL >
    %
    SELECT
        country,
        city,
        browser,
        os,
        device
    FROM
        pm_click_events
    WHERE
        view_id = {{ String(viewId, required=True) }}
    ORDER BY
        timestamp DESC
    LIMIT 1

TYPE ENDPOINT

TYPE ENDPOINT
