VERSION 1

NODE endpoint
SQL >
    %
    SELECT
        documentId,
        SUM(duration) AS sum_duration
    FROM
        page_views
    WHERE
        documentId IN splitByChar(',', {{ String(documentIds, required=true) }})
        AND time >= {{ Int64(since, required=true) }}
    GROUP BY
        documentId

TYPE ENDPOINT
