// Confidential view is a plan-gated premium feature that's out of scope for
// this deployment (no billing tiers are configured), so the section that
// would let someone enable it in the link-creation sheet is hidden entirely.

export default function ConfidentialViewSection(
  _props: Record<string, unknown>,
) {
  return null;
}
