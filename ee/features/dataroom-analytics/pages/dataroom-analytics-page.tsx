// Datarooms (and their dedicated analytics dashboard) are out of scope for
// this deployment — only single-document analytics is supported.

export default function DataroomAnalyticsPage() {
  return (
    <div className="p-8 text-center text-sm text-muted-foreground">
      Dataroom analytics isn&apos;t available in this deployment.
    </div>
  );
}
