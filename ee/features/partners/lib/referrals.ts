// The partner/referral program is out of scope for this deployment, so the
// "Earn and Refer" menu item it gates is always hidden.

export function isReferralsEnabled(): boolean {
  return false;
}
